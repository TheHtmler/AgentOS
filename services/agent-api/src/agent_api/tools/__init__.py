"""Agent-callable tools registered with the Pydantic AI runtime.

Implementations live under capability-domain packages (`search/`, `fetch/`, …).
Mounting and allow/ask/deny decisions go through `registry` + `policy`.
"""
