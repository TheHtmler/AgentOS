# Mac mini / local: Agent API on loopback (BFF only; browser never sees this).
AGENT_API_BASE_URL=http://127.0.0.1:8100

# Production ops host (for operators): https://ops-agentos.lemonbabycare.cn
# Deploy: docs/14-macmini-frp-ops-deploy.md
