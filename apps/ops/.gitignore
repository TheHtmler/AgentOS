# next-env.d.ts is regenerated by every `next build`/`next dev`; with deploys
# building into .next.new (NEXT_DIST_DIR) its routes import flip-flops between
# dist dirs and dirties the working tree. Ignored, same as apps/web.
next-env.d.ts
